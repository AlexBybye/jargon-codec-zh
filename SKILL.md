---
name: jargon-codec-zh
description: 在中文普通表达与互联网大厂黑话之间进行双向忠实转换，覆盖产品、运营、商业、研发、管理与 AI 语境，并支持在“无所不用其极”档位进行每句仅混入一至两个英文词的 bilingual code-switching。仅在用户明确要求把已有原文改成黑话、大厂话、互联网话、普通话或人话，或明确指定“自然”“无所不用其极”、bilingual、关键词双语时使用；不要用于普通润色、摘要、外语翻译、事实核查、技术问答、代码任务、动机判断或无原文的随机黑话生成。
---

# 普通话与大厂黑话双向转换

## 执行流程

1. 确认任务是“大厂黑话 → 普通话”或“普通话 → 大厂黑话”。普通润色、摘要、翻译、核查、技术／代码任务及无原文生成不属于本 Skill。
2. 判断方向。用户未说明且无法从请求判断时，只追问方向。
3. 普通话转大厂黑话时处理档位：
   - `自然`：自然、克制、真实工作可用；
   - `无所不用其极`：语义连贯且事实忠实，黑话密度与大厂腔推到极致；可叠加 `bilingual`，在中文句子中自然混入一至两个英文词。
   用户未指定时只给出这两个选择，不先行改写或暗中代选。黑话转普通话不要求档位。
4. 读取 [references/glossary.md](references/glossary.md)，按领域和上下文检索词条；不要逐词替换或随机堆词。
5. 在内部列出必须保留的事实：数字、单位、时间、人物、组织、地区、对象、范围、顺序、因果、条件、例外、否定、负责人、截止时间、指标口径、情态和承诺强度。
6. 原样保护代码、命令、路径、URL、Markdown 链接目标、结构化键名以及用户指定不可改内容。
7. 完成转换后逐项核对事实。纯转换默认只给结果；仅在用户要求解释或歧义会改变含义时补充一句说明。

## 转成普通话

- 把抽象包装还原成“谁做什么、为谁解决什么、何时完成、如何判断”的直接中文。
- 展开妨碍理解的缩写；精确技术词保留原名并给短释义。AI 术语也是大厂黑话的一部分，不另设模式。
- 把“赋能、抓手、闭环、拉通、沉淀、链路”等还原成具体动作；原文没有细节时保持概括，不自行补流程。
- 对公司别称、岗位缩写、优先级和统计口径按语境解释；缺少上下文时列出可能含义，不给唯一结论。
- 不把招聘话术或管理表达直接判定为恶意潜台词；讽刺含义只能标成可能。

## 转成大厂黑话

### `自然`

- 每句通常只引入一至两个真正增加表达精度的术语。
- 优先使用对齐口径、明确 owner、灰度、回滚、复盘、沉淀等真实工作表达。
- 不堆同义词，不把短句扩成空泛宣言。

### `无所不用其极`

- 可密集使用“围绕、聚焦、牵引、抓手、链路、闭环、场域、沉淀、外推”等组织表达。
- 可以重组句式和强化包装感，但不得新增项目阶段、上下游、架构、数据来源、价值、业绩、指标、权限或承诺。
- 只有原文确实覆盖完整过程时才用“端到端、全链路、闭环”；存在小范围验证时才用“灰度、试点”；存在推广含义时才用“复制、规模化”。
- 此档位允许叠加 `bilingual`：用户要求 bilingual、关键词双语或中英混排时，每个句子只把一至两个适合职场混用的词直接换成英文，例如“先 `sync` 一下目标，赵宁作为 `owner` 牵引交付闭环”。
- 不使用 `中文（English）` 注释，不把整串黑话翻译成英文，不在同一位置保留中英文同义词；大厂黑话骨架仍以中文为主。
- 优先选择自然融入句子的 `sync`、`alignment`、`owner`、`focus`、`scope`、`priority`、`timeline`、`review`、`feedback`、`delivery`、`risk` 等词。原文已有英文缩写或技术术语时直接保留，不为凑数额外替换。
- 保留全部限制语，不把“计划、可能、可以”升级为“完成、保证、必须”，不弱化“不得、仅、低于、除非”。

## 语义底线

- 不新增看似合理的常识、提升比例、成本、能力、共识、领先结论或合规判断。
- 区分相关与因果、局部样本与整体结论、计划与完成、建议与强制。
- 不自行推定 GMV、活跃用户、转化率、准确率等指标的统计范围。
- `RAG` 不等于准确，`MCP` 不等于工具自动可用，`Agent` 不等于完全自主，`多模态` 不等于所有模态都能输入输出。
- 不增删负责人、审批者、人工确认、风险承担者或截止时间。
